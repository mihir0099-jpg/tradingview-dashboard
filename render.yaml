services:
  - type: web
    name: tradingview-dashboard
    env: node
    plan: free
    buildCommand: "npm run build"
    startCommand: "node backend/server.js"
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3002
